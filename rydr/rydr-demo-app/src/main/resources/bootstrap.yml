spring:
  application:
    name: rydr-demo-app
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: dev
      label: master
      fail-fast: false
      allow-override: true
      override-none: true
